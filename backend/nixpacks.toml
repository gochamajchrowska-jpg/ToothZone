[phases.setup]
nixPkgs = ["nodejs_20", "python312", "python312Packages.pip"]

[phases.install]
cmds = [
  "npm install",
  "python3 -m pip install -r requirements.txt"
]

[start]
cmd = "node server.js"
